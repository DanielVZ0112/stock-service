server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stock_service
    username: root
    password: vasco01122117874

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
